/**
 * Invoca a la función con el valor de parámetros adecuados, para que se muestre por consola el valor "10". SOLO puedes modificar el valor de las variables sum1 y sum2.
 */

 function suma(a, b) {
     return a + b
 }

 let suma1 = 4;
 let suma2 = 6;
 let resultado = suma(suma1, suma2)
 console.log(resultado)



 // Te has fijado que la función no tiene por que devolver el valor de una variable? Puede devolver cualquier evaluación de una expresión, en este caso, el resultado de evaluar a + b